{
  "name": "@aioi/notification-service",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "description": "Alerts, briefs, and signed webhooks (email/Slack/Discord/Telegram).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@aioi/shared": "workspace:*",
    "@aioi/validation": "workspace:*",
    "@aioi/logger": "workspace:*"
  },
  "devDependencies": {
    "@aioi/eslint-config": "workspace:*",
    "@aioi/tsconfig": "workspace:*"
  }
}
