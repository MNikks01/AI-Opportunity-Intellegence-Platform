{
  "name": "@aioi/scheduler",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Cron + BullMQ repeatable jobs that drive ingestion and trend recompute.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@aioi/ai-service": "workspace:*",
    "@aioi/database": "workspace:*",
    "@aioi/email": "workspace:*",
    "@aioi/ingestion-service": "workspace:*",
    "@aioi/logger": "workspace:*",
    "@aioi/shared": "workspace:*",
    "@aioi/validation": "workspace:*",
    "bullmq": "^5.80.8",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@aioi/eslint-config": "workspace:*",
    "@aioi/tsconfig": "workspace:*"
  }
}
