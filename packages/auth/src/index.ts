/**
 * @aioi/auth
 * Auth adapter (Clerk today) + RBAC permission checks. Swappable behind this interface.
 *
 * Skeleton stub (Phase 17). Implementation begins in Phase 23 per docs/09-process/ROADMAP.md.
 */
export const PACKAGE_NAME = "@aioi/auth" as const;
