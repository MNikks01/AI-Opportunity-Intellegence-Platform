{
  "name": "@aioi/docs",
  "version": "0.0.2",
  "private": true,
  "description": "Product + API documentation and help center.",
  "//": "Stub app — not yet implemented. Build/lint/typecheck scripts are added when work starts (turbo skips packages without them).",
  "scripts": {
    "clean": "rm -rf .next .turbo"
  },
  "dependencies": {
    "@aioi/ui": "workspace:*",
    "@aioi/shared": "workspace:*"
  },
  "devDependencies": {
    "@aioi/eslint-config": "workspace:*",
    "@aioi/tsconfig": "workspace:*"
  }
}
